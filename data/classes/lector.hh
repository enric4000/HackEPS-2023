// #include "stdafx.h"
#include <iostream>

using namespace std;



    /*
    cout << "x;y;description;picking_x;picking_y" << endl;
    bool primer = true;
    for (int i = 0; i < Mat.size(); ++i)
    {
        primer = true;
        for (int kk = 0; kk < (Mat[i].size()); ++kk)
        {
            if (primer)
                primer = false;
            else
                cout << ';';
            cout << Mat[i][kk];
        }
        cout << endl;
    }
    */